- `effort_ratio` 現在預設所有學生都是 `0.5`，是否要從資料檔提供每位學生的努力值，或由使用者互動輸入？
- 名次目前以「修正後總分」排序，同分時以 `id` 排序；是否需要改成同分同名次（競賽排名）？
- 不及格條件目前是「修正後平均 < 60」，是否要改成「任一科 < 60」或「原始分也必須及格」等複合規則？
- 輸出到外部檔案
- 讓相同程式可重用於不同班級與不同及格線。
- 提供 `dense` / `competition` / `ordinal` 三種排名模式。
- 班級摘要加入標準差與中位數。
